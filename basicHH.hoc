load_file("stdrun.hoc")

create soma
access soma
insert hh
objref tvec,vvec,stim
stim = new IClamp(0.5)
{stim.dur=10 stim.del=2 stim.amp=0}
tstop=50
tvec = new Vector()
vvec = new Vector()
tvec.record(&t)
vvec.record(&soma.v(0.5))

run()
print vvec.max()
run()
print vvec.max()
